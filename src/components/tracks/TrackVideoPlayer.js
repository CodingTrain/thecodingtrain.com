import React, { memo } from 'react';

import * as css from './TrackVideoPlayer.module.css';

const TrackVideoPlayer = ({ chapters }) => {
  return <div className={css.root}></div>;
};

export default memo(TrackVideoPlayer);
